# SPEC-GAPS

This file tracks Vue/Nuxt parity gaps after the 2026-07-31 implementation run.
It is intentionally scoped to `frontend-vue/` tests and does not claim full
product parity until a real-backend/manual pass signs off the remaining areas.

## Closed Or Anchored

| Area | Current Vue Coverage | Status |
| --- | --- | --- |
| Nuxt P0 harness | `tests/p0/*`, `tests/guards/no-vue-in-stream-core.test.ts`, `tests/guards/no-disabled-checks.test.ts`, `tests/contract/proxy-policy.test.ts` | Closed for P0 harness, no-Vue-in-stream-core guard, and no-disabled-checks guard |
| Nitro proxy / SSE / CSRF | `tests/contract/proxy-policy.test.ts`, `tests/unit/core/api/csrf.test.ts`, stream client/transport tests | Anchored; real gateway smoke still belongs to deployment validation |
| Playwright/Nuxt webServer | `scripts/e2e-nuxt-dev-server.mjs`, `playwright.vue.config.ts`, `tests/e2e/*` | Closed for local fixed-port E2E harness |
| Minimal chat E2E | `tests/e2e/chat-flow.spec.ts`, `tests/e2e/utils/mock-api.ts` | Anchored with mocked backend chat flow |
| Chat vertical smoke | `tests/unit/pages/workspace-chat.nuxt.test.ts`, `tests/unit/core/api/thread/client.test.ts`, `tests/unit/composables/use-thread-list.test.ts`, `tests/unit/stores/thread-stream.test.ts`, `tests/unit/core/api/stream/client.test.ts`, `tests/contract/gateway-sse-resume.test.ts` | Anchored for Gateway-backed new-thread creation and cache/nav update, send draft/error behavior, stop refresh behavior, Gateway-shaped SSE message/run/cursor/gap/error state, stream status live region, active sidebar `aria-current`, skip-to-chat-content anchor, artifact panel route isolation, and settings/agent context handoff to runs |
| Workspace chat routes/list | `tests/unit/pages/workspace-chat.nuxt.test.ts`, thread list/history composable tests | Closed for active route, new chat, load older, pin/rename/delete basics |
| Thread API error envelopes | `tests/unit/core/api/thread/client.test.ts` | Closed for non-JSON, empty, array, and object detail fallback |
| Pinned sort + pagination dedupe | `tests/unit/composables/use-thread-list.test.ts` | Closed |
| Thread history page behavior | `tests/unit/pages/workspace-chat.nuxt.test.ts`, `tests/unit/composables/use-thread-history.test.ts` | Anchored for load more, live overlay, route isolation |
| Stream replay/gap state | `tests/unit/pages/workspace-chat.nuxt.test.ts`, stream reducer/store tests | Anchored for warning, cursor, runId, and gap count |
| Gateway SSE resume contract smoke | `tests/contract/gateway-sse-resume.test.ts`, `tests/unit/core/api/stream/client.test.ts`, `tests/unit/core/api/stream/sse-transport.test.ts`, `tests/unit/stores/thread-stream.test.ts` | Anchored for `stream_replay_gap`, retained/dropped replay history source semantics, durable `/state` reload, start/join gap recovery, `/join` with `Last-Event-ID`, preserving caller `Headers` while adding SSE `Accept`, duplicate event-id dedupe, and recovering status |
| Real Gateway SSE replay live smoke | Live smoke 2026-08-02 through Vue Docker same-origin API | Closed for real Gateway run start, retained `/join` replay with `Last-Event-ID`, dropped replay `stream_replay_gap` from `Last-Event-ID: 0-0`, durable `/state` reload availability, and recovery `/join` from `latest_available_event_id` to `end` |
| Docker Vue/Nginx proxy parity | `tests/contract/docker-vue-parity.test.ts`, `tests/contract/proxy-policy.test.ts`, `docker-vue/README.md` | Anchored for Vue compose network/port/Gateway env, explicit Nitro `0.0.0.0:3000` runtime, Docker build/runtime artifact separation, runtime healthcheck against public `/login`, hot-reload Docker dev compose without shadowing container `node_modules`, Nuxt/Nitro `/api/langgraph/**` and `/api/**` routeRules, Gateway env precedence, Mermaid/dayjs production-build plugin aliasing, and legacy nginx rewrite parity |
| SPEC-GAPS handoff consistency | `tests/contract/spec-gaps.test.ts`, `tests/SPEC-GAPS.md` | Anchored for resolvable Vue test/script/docker-vue references, unique Closed/Anchored area labels, unique Open Gap labels, no reopened closed areas, and required exit criteria on every open row |
| Message rendering minimum | `tests/unit/components/workspace/messages/message-list.test.ts` | Anchored for human/ai/tool/error grouped rendering plus basic assistant tool-call and tool-result rich cards |
| Message rich rendering thin layer | `tests/unit/core/messages/rich-content.test.ts`, `tests/unit/core/messages/tool-cards.test.ts`, `tests/unit/components/workspace/messages/message-list.test.ts`, `tests/unit/core/artifacts/utils.test.ts` | Anchored for paragraphs, CommonMark ATX h1-h6 headings with closing-sequence trimming, blockquotes, CommonMark thematic breaks, unordered/ordered lists, React Streamdown `remark-gfm`-backed task list checkboxes, React Streamdown `remark-gfm`-backed double-tilde strikethrough with single-tilde and longer-tilde-run preservation, React Streamdown `remark-gfm`-backed URL/email autolink literals (`http`/`https`, `www.`, and email) with trailing punctuation preservation and code/raw-HTML exclusion, React Streamdown `remark-gfm`-backed footnote references/definitions with code exclusion and a safe Vue footnote section, CommonMark reference-style links/images with hidden definitions, case/whitespace-normalized labels, artifact-aware images, safe/unsafe link handling, and code/raw-HTML exclusion, CommonMark numeric and common named character references plus escaped punctuation in visible text/link labels/image alt text with code/raw-HTML exclusion, React Streamdown/common-mark-backed `**strong**` and `*emphasis*` inline markers with nested code/link/math/strikethrough content and code/raw-HTML exclusion, CommonMark hard line breaks from trailing two-space and backslash markers with soft line breaks preserved, React Streamdown `rehypeStreamingListItems`-backed streaming list marker behavior for trailing empty list items and stable ordered counters, React Streamdown `remark-gfm`-backed tables including divider-row left/right/center alignment plus escaped/code pipe cell delimiter preservation, code fences with normalized language labels and copy action, inline code, safe/unsafe links, citation labels, deduplicated citation source panel, artifact links, artifact-aware image URLs, KaTeX-backed inline/display math with React Streamdown-compatible delimiter normalization plus `remark-math`-backed inline `$...$` and single-line `$$...$$` display math with code/raw-HTML/fenced-code exclusions, React Streamdown-backed pathological blockquote/list nesting caps, code-aware leaked system marker stripping, a trusted VNode renderer for sanitized KaTeX/safe inline HTML without `v-html`, Mermaid fence detection with React-backed labelled dotted-arrow normalization, source-backed Mermaid rendering through a trusted SVG VNode renderer and source fallback, active assistant streaming reveal markers that only animate appended inline tails while settled history stays static, browser/tool call labels, skill install tool-call summaries, staged artifact write summaries, tool result summaries, and write-file/append draft card previews |
| Human input v1/v2 | `tests/unit/core/messages/human-input.test.ts`, `tests/unit/components/workspace/messages/human-input-card.test.ts`, page/store tests | Anchored for v1 option/text/other, v2 form, hidden reply `additional_kwargs`, and form/text validation errors exposed through `role=alert`, `aria-describedby`, and `aria-invalid` |
| Artifact panel minimum | `tests/unit/composables/use-artifact-panel.test.ts`, page tests | Anchored for open/select/session restore, route isolation, detail-level artifact selection, and detail close/reopen preserving the selected artifact |
| Artifact preview/download/viewer status | `tests/unit/core/artifacts/utils.test.ts`, `tests/unit/composables/use-artifact-panel.test.ts`, `tests/unit/pages/workspace-chat.nuxt.test.ts` | Anchored for API/download URL construction, browser preview kind including code/text files, React-backed source language mapping including `.skill`/config/log files, image/audio/video/PDF/HTML/Markdown preview, sandboxed PDF iframe behavior, sandboxed HTML blob preview with source-loaded relative resource rewrite, same-origin artifact resource collection/inlining, injected HTML preview scroll save/restore wiring without `allow-same-origin`, Markdown artifact preview through the safe rich-content renderer, HTML preview/code toggle with observable pressed state, code/text content loading, code loading/error/selection isolation state, keyboard-focusable read-only CodeMirror source viewer region, selected artifact `aria-current`, panel `aria-expanded`/`aria-controls`, source-copy/install/loading/error live-region semantics, source-copy feedback, code viewer filename/language/line-count chrome, download filename, open/download/copy links, copy status reset on selection change, restored/presented artifact retention in detail options when discovery omits the current file, narrow-screen artifact controls/previews remaining operable within the current Vue page layout, skill archive install action through `/api/skills/install`, non-preview fallback, media/PDF/browser preview fallback descriptions, and write-file/append draft preview description |
| Goal command/status | `tests/unit/composables/use-thread-goal.test.ts`, `tests/unit/core/api/thread/client.test.ts`, page tests | Anchored for GET/PUT/DELETE `/goal` and optimistic display |
| Compact command guard | `tests/unit/composables/use-thread-compaction.test.ts`, API client tests, page tests | Anchored for empty-thread skip, busy disable, success refetch, and error display |
| Local thread settings | `tests/unit/core/settings/local.test.ts`, `tests/unit/composables/use-local-thread-settings.test.ts`, page tests | Anchored for persisted allowed context fields and send context |
| Auth/setup/callback/session | `tests/unit/core/auth/client.test.ts`, `tests/unit/pages/auth-pages.nuxt.test.ts`, `tests/unit/pages/settings.nuxt.test.ts`, `tests/contract/auth-state.test.ts`, `tests/e2e/smoke.spec.ts`, `tests/e2e/auth-flow.spec.ts` | Anchored for key UI, safe `next`, login redirect construction, login/setup request shape, login/setup validation error associations, callback status/error live-region semantics, account password/logout shape, failed-logout hard redirect, Nitro route protection, browser local-login form submission, Gateway error display, password-change CSRF, rotated CSRF reuse on logout, and protected redirect after cookie clearing |
| Scheduled tasks MVP | `tests/unit/core/api/scheduled-tasks/client.test.ts`, `tests/unit/composables/use-scheduled-tasks.test.ts`, `tests/unit/pages/scheduled-tasks.nuxt.test.ts` | Anchored for list, thread scoped create, runs, pause/resume/trigger/delete |
| Scheduled tasks once create | `tests/unit/core/api/scheduled-tasks/client.test.ts`, `tests/unit/pages/scheduled-tasks.nuxt.test.ts` | Anchored for cron/once create toggle, timezone wall-clock `datetime-local` to backend `schedule_spec.run_at`, and backend one-time create payload |
| Scheduled tasks edit/filter UX | `tests/unit/core/api/scheduled-tasks/client.test.ts`, `tests/unit/composables/use-scheduled-tasks.test.ts`, `tests/unit/pages/scheduled-tasks.nuxt.test.ts` | Anchored for `PATCH /scheduled-tasks/{task_id}`, edit form for locked schedule type including timezone wall-clock once edits, status/type filters, selected-task reselection, recipes, cron presets, timezone catalog, parsed cron/create preview, backend lease/overlap/run-count/last-run fields in the detail model, manual-trigger run-history refresh, active-run conflict display, and skipped terminal run detail |
| Scheduled tasks structured scheduling UX | `tests/unit/pages/scheduled-tasks.nuxt.test.ts`, `tests/contract/scheduled-backend-contract.test.ts` | Anchored for structured hourly/daily/weekdays/weekly/monthly cron builder controls, builder-to-5-field-cron payloads, timezone-aware wall-clock calendar affordances for once schedules, Gateway timezone semantics copy, create/edit validation alert associations, schedule/filter `aria-pressed` states, selected task `aria-current`, user-facing status/trigger labels, formatted run-history timestamps, and edit-form schedule previews without changing the real Gateway scheduler contract |
| Real scheduler poller live smoke | Live smoke 2026-08-02 through Vue Docker same-origin API and Gateway scheduler | Closed for real scheduler enable/restart guard, once timing/poller launch, scheduled run completion, manual overlap active-run conflict, interrupt cleanup, test-task cleanup, and restoring `scheduler.enabled:false` after signoff |
| Agent chat route context | `tests/unit/pages/workspace-chat.nuxt.test.ts`, agent route wrapper pages | Anchored for route `agent_name` context and new/delete path namespace |
| `/workspace/agents/new` | `tests/contract/agents-backend-contract.test.ts`, `tests/unit/core/api/agents/client.test.ts`, `tests/unit/composables/use-new-agent.test.ts`, `tests/unit/pages/agents-new.nuxt.test.ts` | Anchored for name check errors with field alert association, backend bootstrap context keys, flash/thinking-disabled setup context, stream/status live-region feedback, hidden save command, `setup_agent` result readback, normalized agent readback, and created-state navigation |
| Settings shell / appearance / account / about | `tests/unit/core/settings/preferences.test.ts`, `tests/unit/core/auth/client.test.ts`, `tests/unit/core/about/content.test.ts`, `tests/unit/config/antd-theme-locale.test.ts`, `tests/unit/pages/settings.nuxt.test.ts` | Anchored for section navigation, query/hash/dialog deep links, local theme/locale persistence including system fallback, app-level AntD theme/locale state sync, AntD locale/token mapping, document `html lang` mapping, auth profile load, password change, account password validation error/status accessibility, SSO password suppression, logout, About links/features/version fallback, and markdown-style About sections with explicit runtime verification boundary |
| Settings memory management | `tests/unit/core/api/memory/client.test.ts`, `tests/unit/composables/use-memory-settings.test.ts`, `tests/unit/pages/settings.nuxt.test.ts` | Anchored for `/api/memory` load/delete, `/api/memory/export`, `/api/memory/import`, fact create/update/delete client contracts, enabled section gate, query cache updates, page create/edit/delete/clear, JSON import/export, validation, and empty state |
| Settings tools/MCP management | `tests/unit/core/api/mcp/client.test.ts`, `tests/unit/composables/use-mcp-settings.test.ts`, `tests/unit/pages/settings.nuxt.test.ts` | Anchored for `/api/mcp/config` GET/PUT/PATCH, `/api/mcp/cache/reset`, 403 admin-required state, CSRF config writes, section-gated query, cache update, server detail, configured tool overrides list, runtime discovery summary from configured servers/tool hints, explicit non-live schema-signoff note, deep-linkable JSON editor dialog, JSON edit validation, empty, and disabled label |
| Settings skills management | `tests/unit/core/api/skills/client.test.ts`, `tests/unit/composables/use-skill-settings.test.ts`, `tests/unit/pages/settings.nuxt.test.ts` | Anchored for `/api/skills` load, `/api/skills/{name}` detail/toggle, `/api/skills/install`, `/api/skills/reload`, custom content GET/PUT/DELETE/history/rollback, 403 admin-required state, CSRF writes, section-gated query, cache updates, public/custom filter, readonly non-admin toggle, create chat link, deep-linkable custom SKILL.md draft generation without inventing a create endpoint, deep-linkable skill-reviewer command panel, rollback editor refresh, and disabled label |
| Settings notification/channels | `tests/unit/core/settings/preferences.test.ts`, `tests/unit/core/api/channels/client.test.ts`, `tests/unit/pages/settings.nuxt.test.ts` | Anchored for local browser notification preference/permission/test-notification UI and Gateway-shaped `/api/channels/providers`, `/api/channels/connections`, `/{provider}/connect`, deep-linkable `/{provider}/runtime-config`, provider disconnect, and connection revoke contracts |
| Settings integrations/Lark thin layer | `tests/contract/lark-integration-backend-contract.test.ts`, `tests/unit/core/api/integrations/lark.test.ts`, `tests/unit/composables/use-lark-integration.test.ts`, `tests/unit/pages/settings.nuxt.test.ts` | Anchored for `/api/integrations/lark/status`, install/config/auth start/complete endpoints, CSRF integration writes, admin-required install mapping, status host-path redaction contract, section-gated query, install/config/auth cache updates, skills query invalidation, settings status/install UI, and deep-linkable Lark config/auth wizard panels with verification URL, user code, device code, request payload, completion, and error feedback |
| Workspace navigation shell | `tests/unit/pages/workspace-navigation.nuxt.test.ts` | Anchored for shared workspace nav shell, skip-to-main-content link, home cards, agents new link, single-current active `aria-current`, scheduled/settings hrefs, collapsed state persistence, compact density persistence, control accessible labels, keyboard collapse smoke, topbar breadcrumb/GitHub release polish, chats-index recent thread history affordances with pinned/source/agent-route metadata, and distinct recent-chat loading/empty/list states |
| Chinese fixed UI copy | `tests/unit/pages/workspace-navigation.nuxt.test.ts`, `tests/unit/pages/auth-pages.nuxt.test.ts`, `tests/unit/pages/agents-new.nuxt.test.ts`, `tests/unit/pages/workspace-chat.nuxt.test.ts`, `tests/unit/pages/settings.nuxt.test.ts`, `tests/unit/pages/scheduled-tasks.nuxt.test.ts`, message/component/core/composable tests | Anchored for current Vue-owned static UI copy across workspace navigation, auth/setup/callback, chat status/actions, message/human-input/tool cards, Artifact viewer/copy/fallback chrome, Scheduled Tasks, Settings panels, Goal/Compaction, About content, and frontend fallback errors; backend/user/model payload text and product/protocol names remain source values |
| Real-backend auth contract/source smoke | `tests/contract/auth-backend-contract.test.ts`, `tests/unit/composables/use-auth-session.test.ts` | Anchored against the real `backend/app/gateway/routers/auth.py` routes for login/setup/me/logout/change-password/OIDC, plus Vue client-session refresh and protected-route expiry redirect |
| Real scheduler service contract/source smoke | `tests/contract/scheduled-backend-contract.test.ts`, `tests/unit/core/api/scheduled-tasks/client.test.ts` | Anchored against real scheduler router/service/repository/model/migration sources for create/update validation, once/cron timing, leases, global concurrency, overlap skip, active-run conflict, skipped tombstones, non-interactive launch context, terminal completion mapping, and Vue lease/overlap fields |

## Current Batch Convergence Notes

- Current Vue anchors cover auth browser flow, auth/setup/callback status/error semantics, Gateway-backed chat creation, send/stop/error chat flow, page-visible Gateway-shaped SSE state with live-region status, chat sidebar skip/active-current semantics, scheduler lease/run-history display, structured scheduler cron builder and timezone wall-clock affordances, scheduled create/edit validation error associations, schedule/filter pressed states, selected task current state, SSE header preservation, scheduled-task UX helpers, settings query/hash/dialog deep links, settings account password error/status accessibility, settings skill rollback, skill creation draft/review command context, agents/new name validation and status live regions, MCP runtime discovery context from configured servers, notification preference flow, channel provider/connection management UI contracts, Lark config/auth wizard request and completion flows, markdown-style About sections, workspace skip-to-main-content link, workspace breadcrumb/single-active navigation polish, chats-index history/loading/empty affordances, artifact copy and code/text content loading, React-backed artifact language mapping, keyboard-focusable read-only CodeMirror source viewing, artifact panel expanded/current/status semantics, artifact source-copy feedback, artifact code viewer chrome, detail-level artifact selection, detail close/reopen preserving selected artifact, restored/presented artifact detail-option retention, HTML blob preview resource rewrite, same-origin artifact resource inlining, HTML preview scroll save/restore injection, HTML preview/code toggle pressed state, Markdown artifact preview, HTML/PDF/media preview/fallback behavior, narrow-screen artifact control/previews operability within the current Vue page layout plus <=560px control stacking anchors, artifact skill install action, basic browser/tool/staged-artifact rich cards, KaTeX-backed inline/display math including React Streamdown `remark-math`-backed inline dollar and single-line display dollar delimiters, React Streamdown-backed nesting safety preprocessing, CommonMark thematic breaks, CommonMark ATX h1-h6 headings with closing-sequence trimming, React Streamdown `remark-gfm` task list rendering, React Streamdown `remark-gfm` table divider alignment and escaped/code pipe cell delimiter preservation, React Streamdown `remark-gfm` double-tilde strikethrough with single-tilde and longer-tilde-run preservation, React Streamdown `remark-gfm` URL/email autolink literals, React Streamdown `remark-gfm` footnotes, CommonMark reference-style link/image rendering, CommonMark character reference and escaped punctuation rendering in visible text/link labels/image alt text, React Streamdown/common-mark `**strong**` and `*emphasis*` inline marker rendering, CommonMark hard line break rendering, React Streamdown `rehypeStreamingListItems` trailing-empty-list marker behavior, code-aware leaked system marker stripping, sanitized safe inline HTML rendering without `v-html`, Mermaid fence detection/render fallback with labelled dotted-arrow normalization, active assistant streaming reveal tail markers with reduced-motion-safe CSS, citation source panel, Chinese fixed UI copy across current Vue-owned surfaces, document language mapping, and Docker runtime healthcheck.
- Full `$t()` / language-pack replacement is explicitly user-excluded and must not be treated as a remaining implementation recommendation. The typed i18n scaffold may remain, but Vue-owned page static copy should not be migrated into `$t()` dictionaries under the current boundary.
- Live runtime claims are deliberately not closed by mocked browser specs or source-contract tests. Manual visual/a11y behavior still requires configured manual evidence before release signoff. IM provider live and scheduler expired-lease crash/reclaim are explicitly user-excluded and must not be requested as remaining signoff items.
- The Docker live lane now has real Vue container/Nitro runtime evidence: `DEER_FLOW_NETWORK=deer-flow-dev_deer-flow-dev docker compose -f docker-vue/docker-compose.yaml up -d --build` passed after Dockerfile/production-build fixes; `deer-flow-frontend-vue` is healthy on `0.0.0.0:2027->3000`, container logs show Nitro listening on `0.0.0.0:3000`, `curl -I http://127.0.0.1:2027/login` returned 200, and headless Chromium loaded the login page. After `make docker-start` brought up the main dev stack, the Vue image was rebuilt with `NUXT_GATEWAY_URL=http://gateway:8001` as a build arg so Nuxt routeRules no longer bake `127.0.0.1:8001`; `curl -i http://127.0.0.1:2027/api/v1/auth/setup-status` returned real Gateway 200 with `{"needs_setup":false,"registration_enabled":true}`, and Chromium no longer showed the Gateway initialization unavailable alert. A real admin account signed the Vue same-origin auth API/session/protected-route/logout path and Gateway-backed create-chat/send-message path; `/api/threads/{thread_id}/runs/stream` returned 200 after Vue switched its requested stream mode from unsupported `messages` to Gateway-supported `messages-tuple`. Gateway CORS is now persisted in the root `.env` as `GATEWAY_CORS_ORIGINS` for `http://127.0.0.1:2027`, `http://localhost:2027`, `http://127.0.0.1:2028`, and `http://localhost:2028`; both main Gateway compose files document that `env_file: ../.env` injects the split-origin allowlist. Recreating only `deer-flow-gateway` without the temporary override preserved the env, and headless Chromium browser-form login on both `http://127.0.0.1:2027/login?next=/workspace/settings` and `http://127.0.0.1:2028/login?next=/workspace/settings` returned `/api/v1/auth/login/local` 200, navigated to `/workspace/settings`, and `/api/v1/auth/me` returned 200 with matching admin identity.
- Real Gateway SSE replay/resume now has live evidence through the Vue Docker same-origin API: a dedicated run returned `/runs/stream` 200 with `Content-Location` `/api/threads/279e2007-4f94-4f67-819c-c415fc17f998/runs/93eba56e-fe33-458c-bf87-624557e5ddd3`, start stream events included `metadata`, `values`, `messages`, and `end`, retained `/join` with `Last-Event-ID: 1785642845662-0` replayed 22 events through `end`, dropped replay with `Last-Event-ID: 0-0` returned a real `stream_replay_gap` with `earliest_available_event_id: 1785642845662-0` and `latest_available_event_id: 1785642847768-0`, durable `/state` returned 200 with four messages, and recovery `/join` from `1785642847768-0` returned `end` without another gap.
- Real scheduler poller now has live evidence through the Vue Docker same-origin API and Gateway scheduler: after confirming the runtime `scheduled_tasks` table was empty, local gitignored `config.yaml` was temporarily set to `scheduler.enabled:true`, `poll_interval_seconds:2`, and `min_once_delay_seconds:5`, then `deer-flow-gateway` was restarted. A once task `task-59c7240dcb4a405b98ebae70a8d4ac24` scheduled for `2026-08-02T04:20:45Z` was automatically claimed by the poller and completed with task run `task-run-534af5786c824ae6b1bc9dc1559066d9`, `trigger:"scheduled"`, `status:"success"`, run id `661cb93c-2445-4bef-86c1-7af084162d81`, parent task `status:"completed"`, and `run_count:1`. Manual overlap was signed with task `task-3fad739b084b47799fb3ba15fa05ee52`: the first trigger launched running run `73cb32b6-3544-4e87-a4b4-4811ed0461e5`, the second immediate trigger returned 409 `task already has an active run`, and the run was interrupted with HTTP 200 before cleanup. API delete returned 500 for completed/history rows, so the test rows were cleaned directly from SQLite by `title like 'scheduler-live-%'`; post-cleanup test-task count and active scheduled run count were 0. `config.yaml` was restored to `scheduler.enabled:false`, `poll_interval_seconds:5`, `min_once_delay_seconds:60`, and Gateway was restarted again. An artificial expired-lease simulation did not produce a run, is not counted as live signoff, and the user explicitly excluded further scheduler expired-lease crash/reclaim drills.
- The Docker dev lane now has a hot-reload compose entry: `DEER_FLOW_NETWORK=deer-flow-dev_deer-flow-dev docker compose -f docker-vue/docker-compose.dev.yaml up -d --build` starts `deer-flow-frontend-vue-dev` on `0.0.0.0:2028->3000`; `curl -I http://127.0.0.1:2028/login` returned 200, logs show Nuxt dev server and vue-tsc watch running, and the compose config keeps bind mounts scoped to source/config/server files instead of shadowing container `node_modules`.

## Open Gaps

| Area | Scope | Remaining Gap | Required Exit |
| --- | --- | --- | --- |
| Live Gateway auth flow | Live-only | A real admin account signed Vue same-origin auth API login (`POST /api/v1/auth/login/local` 200), session refresh (`GET /api/v1/auth/me` 200), protected settings route load, logout (`POST /api/v1/auth/logout` 200), post-logout `/me` 401, protected-route redirect back to `/login`, and standalone Vue browser-form login on both `127.0.0.1:2027` production Nitro and `127.0.0.1:2028` dev hot-reload runtime after Gateway `GATEWAY_CORS_ORIGINS` was persisted in the root `.env` and injected by the main Gateway compose files. Password-change live is explicitly user-excluded and should not be requested as a remaining live signoff item | No remaining live auth exit required under the current user boundary |
| Docker Vue live container runtime | Live-only | Vue container/Nitro runtime now has real Docker build/up, health status, browser smoke, log evidence, main-stack Gateway proxy evidence, real auth API/session/protected-route/logout evidence, standalone Vue 2027/2028 browser-form login evidence, real create-chat/send-message evidence, real Gateway SSE retained/dropped replay evidence, and real stop/cancel interrupt evidence against the `deer-flow-dev_deer-flow-dev` Docker network. `/api/threads/{thread_id}/runs/stream` returned 200 with real run ids/cursors after the Vue client switched to Gateway-supported `messages-tuple`; stop/cancel clicked the Vue stop button, called `/stream?action=interrupt` with HTTP 200, and backend run `116a2ff9-bb85-49e3-80c9-b796b0a74d4b` persisted status `interrupted` with zero committed messages/tokens | No remaining Docker container runtime live exit under the current user boundary |
| Settings page depth | P2 candidate | `memory`, `skill`, `tool`, `integrations`, `notification`, `channels`, and `about` now have visible anchors; memory has load/create/update/delete/clear/import/export anchors; tools/MCP have list/toggle/admin/detail/config-edit/cache-reset plus config-derived runtime discovery context and deep-linkable config editor anchors; skills have list/toggle/admin/detail/install/reload/custom-edit/delete/history/rollback, React-parity create-chat link to `/workspace/chats/new?mode=skill`, custom SKILL.md draft, and skill-reviewer command anchors; notification/channels have local browser notification and Gateway-shaped channel management anchors; integrations have Lark status/install/config/auth wizard/API contract anchors; about/account/appearance have version/link/SSO/system-theme/markdown-style anchors; account password feedback has alert/status accessibility; query/hash/dialog deep-link parity is source-backed for current Settings dialogs. Full `$t()`/language-pack replacement is user-excluded and not a remaining implementation target. Settings direct-create API/form is not a React parity requirement in the current source-backed lane. IM provider live is user-excluded. Remaining depth includes live runtime MCP schema discovery beyond configured overrides and manual screen-reader/WCAG review beyond source-backed anchors | Live environment evidence for live-only behavior or manual accessibility signoff |
| Workspace navigation depth | P2 candidate | Current Vue workspace shell and chat shell have shared navigation anchors plus skip-to-main-content/skip-to-chat-content links, collapsed/density/keyboard smoke, topbar breadcrumb/GitHub release polish, single-current route highlighting, active chat `aria-current`, recent-chat loading/empty/list affordances with pinned/source/agent-route metadata, and <=560px source-backed control stacking. Remaining depth is manual UX/visual signoff and React `WorkspaceContainer` parity details beyond the Vue shell | Manual UI signoff plus richer E2E navigation smoke |
| Artifact rendering depth | P2 candidate | Vue now has browser preview/open/download/copy/fallback anchors, React-backed source language mapping, keyboard-focusable read-only CodeMirror source viewing, source-copy/status feedback, code/text content loading with loading/error/selection isolation state, artifact panel expanded/current/live-region semantics, basic code viewer filename/language/line-count chrome, detail-level artifact selection, detail close/reopen preserving selected artifact, restored/presented artifact retention in detail options when discovery omits the current file, HTML preview/code toggle with observable pressed state, image/audio/video/PDF/HTML/Markdown preview paths, sandboxed PDF iframe behavior, sandboxed HTML blob preview with source-loaded relative resource rewrite, same-origin artifact resource inlining, injected HTML preview scroll save/restore wiring without `allow-same-origin`, Markdown artifact preview through the safe rich-content renderer, download filename, current-layout narrow-screen artifact controls/previews operability anchors including <=560px source-backed stacking, skill archive install action through the existing skills API client, media/PDF fallback descriptions, and write-file/append draft descriptions including rich cards for streamed tool calls. Remaining depth is no longer a known Release must in the current source-backed lane; it is limited to manual PDF/media visual QA, mobile/resizable drawer visual signoff, and broader React right-panel/sheet/resizable visual parity beyond the current Vue page-layout artifact detail controls | Manual/mobile visual signoff, live artifact samples, or a separately-scoped decision to port React's full resizable right-panel/sheet shell |
| Message rendering depth | P2 candidate | Vue now has a safe Markdown thin layer with CommonMark ATX h1-h6 headings including closing-sequence trimming, blockquotes, CommonMark thematic breaks, unordered/ordered lists, React Streamdown `remark-gfm`-backed readonly task list checkboxes, React Streamdown `remark-gfm`-backed double-tilde strikethrough with single-tilde and longer-tilde-run preservation, React Streamdown `remark-gfm`-backed URL/email autolink literals (`http`/`https`, `www.`, and email) with trailing punctuation preservation and code/raw-HTML exclusion, React Streamdown `remark-gfm`-backed footnote references/definitions with code exclusion and safe Vue footnote rendering, React Streamdown `remark-gfm`-backed table divider alignment plus escaped/code pipe cell delimiter preservation, CommonMark reference-style links/images with hidden definitions, case/whitespace-normalized labels, artifact-aware images, safe/unsafe link handling, and code/raw-HTML exclusion, CommonMark numeric and common named character references plus escaped punctuation in visible text/link labels/image alt text with code/raw-HTML exclusion, React Streamdown/common-mark-backed `**strong**` and `*emphasis*` inline marker rendering with nested code/link/math/strikethrough content and code/raw-HTML exclusion, CommonMark hard line breaks from trailing two-space and backslash markers while preserving soft line breaks, React Streamdown `rehypeStreamingListItems`-backed trailing empty list item hiding, stable ordered counters, and list marker reveal on content arrival, tables, code fences with normalized language labels and copy action, citation labels plus a deduplicated citation source panel, artifact links, artifact-aware images, KaTeX-backed inline/display math with `\(...\)`, `\[...\]`, inline `$...$`, and single-line `$$...$$` delimiter handling plus code/raw-HTML/fenced-code exclusions, React Streamdown-backed pathological nesting caps and code-aware leaked system marker stripping, sanitized safe inline HTML rendering through trusted VNodes, Mermaid code-fence detection, labelled dotted-arrow normalization, trusted SVG VNode rendering, source fallback, and active assistant streaming reveal tail markers, plus browser/tool/staged-artifact rich cards. Remaining depth is no longer a known Release must in the current source-backed thin-renderer lane; it is limited to full Streamdown runtime/component parity beyond the current parser/preprocess/GFM/autolink/footnote/reference/entity/emphasis/hard-break/math/reveal anchors, full HTML5 named entity table parity beyond the current common named subset, `\[...\]` display-math delimiter versus escaped bracket edge semantics, and broad media/manual visual QA | Add P2 renderer/runtime specs only if full Streamdown/component parity becomes a target, or use manual visual signoff for media/edge-case parity |
| i18n/theme/accessibility parity | P2 candidate | Vue now anchors workspace preference persistence, typed `vue-i18n` scaffold/dictionaries, locale-key drift guards, Nuxt i18n plugin state sync, app-level AntD theme/locale sync, locale/token mapping, document `html lang` mapping, settings section `aria-current`, appearance theme `aria-pressed`/labels, nav `aria-current`, skip-to-main-content and skip-to-chat-content navigation, control accessible labels, keyboard collapse smoke, auth/setup/callback status and error semantics, account/human-input/scheduled-task validation alert wiring, chat/artifact live-region feedback, selected task/artifact current semantics, scheduled-task status/trigger/schedule labels, <=560px source-backed responsive control stacking, and current Vue-owned fixed UI copy in Chinese. Full `$t()`/language-pack replacement is user-excluded and not a remaining implementation target; backend/user/model payload text and product/protocol names are still source values. Remaining depth is manual screen-reader/WCAG and manual/mobile visual signoff beyond source-backed anchors | Manual screen-reader/mobile visual signoff before claiming accessibility or visual parity |

## Notes

- `corepack pnpm test` currently covers unit, P0, contract, and guard tests.
- `tests/contract/spec-gaps.test.ts` checks that this handoff map does not
  point at missing Vue test/script/docker-vue files, that Closed/Anchored and
  Open Gap labels remain unique, that every open row has a valid scope, and
  that every open row keeps an explicit exit criterion.
- `corepack pnpm test:e2e` requires a local Nuxt/Playwright webServer. In the
  managed sandbox it may need elevated execution to listen on `127.0.0.1`.
- This file should be updated whenever a gap is closed or deliberately scoped
  out, so future windows do not re-litigate already anchored areas.
