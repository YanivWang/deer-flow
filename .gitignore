# DeerFlow docker image cache
docker/.cache/
# oh-my-claudecode state
.omc/
# OS generated files
.DS_Store
*.local
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python cache
__pycache__/
*.pyc
*.pyo

# Virtual environments
.venv
venv/

# Benchmark outputs
bench_results.jsonl
bench_optimized.jsonl
results.jsonl
backend/scripts/benchmark/*.jsonl

# Environment variables
# .env

# Configuration files
# config.yaml
# mcp_config.json
# extensions_config.json

# IDE
.idea/
.vscode/

# Coverage report
coverage.xml
coverage/
.deer-flow/
.claude/
skills/custom/*
logs/
log/
debug.log

# Local git hooks (keep only on this machine, do not push)
.githooks/

# pnpm
.pnpm-store
sandbox_image_cache.tar

# ignore the legacy `web` folder
web/

# Deployment artifacts
backend/Dockerfile.langgraph
config.yaml.bak
.playwright-mcp
/frontend/test-results/
/frontend/playwright-report/
# frontend-vue/scripts/standalone-sim.mjs 跑到一半崩掉时 frontend/ 的落脚点。
# 正常路径下它只存在几十秒，下一次 standalone-sim 启动会自愈。
/.frontend-standalone-sim-parked/
.gstack/
.worktrees

# Monocle agent-observability trace output (local file exporter)
.monocle/
