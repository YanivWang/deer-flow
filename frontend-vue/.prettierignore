# 生成物：由 scripts/gen-api-types.mjs、scripts/i18n-manager.mjs 等写出。
# prettier 会把短数组折成一行，JSON.stringify 不会——两边都格式化就会让对应的
# `*-check` 门禁在一次 `make format` 之后立刻变红。生成器是唯一权威。
baseline/

# 由 pnpm 维护，格式化它只会与 pnpm 的写法互相打架。
pnpm-lock.yaml

# 生成物：由 scripts/gen-api-types.mjs 写出（openapi-typescript）。
app/core/api/types.gen.ts

# 生成物：由 scripts/gen-contract-constants.mjs 从 <repo>/contracts/*.json 写出。
app/core/contracts/backend.gen.ts
